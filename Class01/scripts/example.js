console.log("Im from example js");

// Excercise 1

/* Write a JavaScript program to convert a length given in feet to meters.
TIP: 1 feet = 0.3048 meters */

// this is how much feet is in meters
let feetPerMeter = 0.3048;

// the value of feet that we want to convert
let feet = 100;

// the math expresion for conversion
let converted = feet * feetPerMeter;

// print result
console.log(converted);

// Excersise 2
/* Write a JavaScript program that will read the two sides of a rectangle and calculate its area
TIP: area = a * b */

// Side values of the rectangle
let a = 10;
let b = 15;

// the math expresion for calculating area
let area = a * b;

// print result
console.log(area);